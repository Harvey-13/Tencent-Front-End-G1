import { RouteObject } from 'react-router-dom';
import homeRouter from './home';
// import loginRouter from './login';
// const {loginStatus} = require('../api/login')
const router: RouteObject[] = [homeRouter];

export default router;